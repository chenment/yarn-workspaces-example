{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "yarn && lerna bootstrap",
    "lernalist": "lerna ll",
    "changed": "lerna changed",
    "test": "lerna run test --scope=@yarn-workspaces/common",
    "new-version": "lerna version -- conventional-commits --yes",
    "diff": "lerna diff"
  },
  "name": "yarn-workspaces",
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
