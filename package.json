{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "lerna run test --scope=@yarn-workspaces/common",
    "new-version": "lerna version --scope=@yarn-workspaces/common -- conventional-commits --yes",
    "diff": "lerna diff"
  },
  "name": "yarn-workspaces",
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
